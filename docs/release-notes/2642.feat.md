Add write compatibility profiles: {attr}`anndata.settings.write_compat` (and a `compat` argument on the write functions and {meth}`~anndata.AnnData.unwriteable`) selects which anndata version’s write behavior to target, see {doc}`/write-compat`.
Passing `compat="0.12"` re-allows writing 3-dimensional {attr}`~anndata.AnnData.X`/{attr}`~anndata.AnnData.layers` ({issue}`2430`) and `/` in `h5ad` keys ({issue}`2039`).
{attr}`anndata.settings.disallow_forward_slash_in_h5ad` is deprecated in favor of it {smaller}`P Angerer`
