Add write compatibility profiles: a `compat` argument on the write functions and {meth}`~anndata.AnnData.unwriteable` selects which anndata version’s write behavior to target ({class}`~anndata.WriteCompat`), see {doc}`/write-compat` {smaller}`P Angerer`
