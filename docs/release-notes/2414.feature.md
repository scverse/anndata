Support `pairwise=True` in {func}`~anndata.experimental.concat_on_disk`, writing the block diagonal of the concatenation-axis pairwise elements (`.obsp`/`.varp`) as {func}`~anndata.concat` does. The blocks are written one object at a time, so the full result is never held in memory {user}`alejandro-publius`
