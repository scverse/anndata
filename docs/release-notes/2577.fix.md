Fix a number of latent crashes surfaced by type checking, among them reading a backed `zarr` element (an internal `filename` lookup always raised `AttributeError` since zarr v3), `read_elem` on an attribute with a default, `obs_keys()`/`var_keys()` on xarray-backed objects, `write_loom` emitting a `None`-named layer, and `__sizeof__` on a COO matrix in `uns`
