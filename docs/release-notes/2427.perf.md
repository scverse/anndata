{attr}`anndata.settings.auto_shard_zarr_v3` now utilizes zarr's support for a target shard via {attt}`zarr.config.set({"array.target_shard_size_bytes" ...})` (only `zarr` version `>=3.1.4`) to make the target shard size 1GB (uncompressed) if not otherwise set. {user}`ilan-gold`
