(v0.12.15)=
### 0.12.15 {small}`2026-05-18`

#### Bug fixes

- Make sure `indices`, `data`, and `indptr` have `zarr.config.set({'array.target_shard_size_bytes'})` applied instead of having one override the other's setting when writing sparse matrices. {user}`ilan-gold` ({pr}`2445`)
- Don't warn when `shards` are explicitly set but {attr}`anndata.settings.auto_shard_zarr_v3` is not {user}`ilan-gold` ({pr}`2451`)
