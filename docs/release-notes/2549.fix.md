Preserve {attr}`~anndata.AnnData.X` (stored as `layers[None]`) when clearing or reassigning {attr}`~anndata.AnnData.layers`. `adata.layers = {}` and `adata.layers.clear()` no longer silently discard `.X`, matching the existing behavior of `del adata.layers`. `.X` is now removed only via explicit `.X`/`None`-key operations. {user}`slambert-git`
