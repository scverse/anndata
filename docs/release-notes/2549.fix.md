Keep {attr}`~anndata.AnnData.X` (stored as `layers[None]`) when clearing or reassigning {attr}`~anndata.AnnData.layers`, matching the existing behavior of `del adata.layers`. `adata.layers = {}`, `adata.layers.clear()` and `del adata.layers` now warn that a future release may drop `.X` in these cases without more expliit handling i.e., pass `adata.layers.clear(keep_x=True/False)` or include the `None` key explicitly (`adata.layers = {None: adata.X, ...}` to keep `X` and `{None: None, ...}` to drop) to opt in and silence the warning. {user}`slambert-git`
