Enforce the 2-dimensional {attr}`~anndata.AnnData.X`/{attr}`~anndata.AnnData.layers` spec at the IO boundary: higher-dimensional arrays now warn (rather than error for {attr}`~anndata.AnnData.X`, and silent accept for {attr}`~anndata.AnnData.layers`) in memory, writing one raises a clear error, and reading a non-conforming file warns instead of silently loading it. {user}`eroell`
