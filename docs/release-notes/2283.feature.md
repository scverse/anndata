Add `.cat` accessor to lazy AnnData columns mimicking pandas API: `.categories` returns `LazyCategories` with efficient slicing (e.g., `categories[:10]`), `.codes` returns the underlying codes, `.ordered` returns whether the categorical is ordered. This enables efficient HTML repr for lazy AnnData with many categories {user}`katosh`
