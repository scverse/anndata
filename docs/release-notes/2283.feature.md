Add `.cat` accessor to lazy AnnData columns with `n_categories`, `get_categories(indices)`, and `categories` for efficient partial access to category information without loading all data {user}`katosh`
