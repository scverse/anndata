(v0.13.1)=
### 0.13.1 {small}`2026-07-08`

#### Documentation

- Move the concatenation guide into the tutorials section {smaller}`Zethson` ({pr}`2539`)

#### Bug fixes

- Lower bound `typing_extensions` module to contain the `sentinel` object {user}`ilan-gold` ({pr}`2540`)
- Accept array-API arrays that cannot export via DLPack (e.g. [pydata/sparse](https://github.com/pydata/sparse)) in {attr}`~anndata.AnnData.X`/{attr}`~anndata.AnnData.layers`. {class}`~anndata.types.SupportsArrayApi` is split into a DLPack-free {class}`~anndata.types.SupportsArrayApiBase` (used to gate storage) and the DLPack-requiring {class}`~anndata.types.SupportsArrayApi` (used only where DLPack is actually needed). {user}`eroell` ({pr}`2541`)
