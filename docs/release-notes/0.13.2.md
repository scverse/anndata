(v0.13.2)=
### 0.13.2 {small}`2026-07-13`

#### Bug fixes

- Keep {attr}`~anndata.AnnData.X` (stored as `layers[None]`) when clearing or reassigning {attr}`~anndata.AnnData.layers`, matching the existing behavior of `del adata.layers`. `adata.layers = {}`, `adata.layers.clear()` and `del adata.layers` now warn that a future release may drop `.X` in these cases without more expliit handling i.e., pass `adata.layers.clear(keep_x=True/False)` or include the `None` key explicitly (`adata.layers = {None: adata.X, ...}` to keep `X` and `{None: None, ...}` to drop) to opt in and silence the warning. {user}`slambert-git` ({pr}`2549`)
- Allow {meth}`~anndata.acc.AdAcc.resolve` to resolve `AdAcc`s (e.g. `A.X`, `A.layers['y']`, `A.obsm['c']`, `A.obsp['g']`) in addition to `AdRef`s,
  when selected via `vec=False|None` (Resolve to `AdRef`s with `vec=True|None`) {user}`flying-sheep` ({pr}`2550`)
- Change `layers: None` in the repr to be `layers: None (.X)` to highlight that `None` is `X` {user}`ilan-gold` ({pr}`2553`)
