Accept array-API arrays that cannot export via DLPack (e.g. [pydata/sparse](https://github.com/pydata/sparse)) in {attr}`~anndata.AnnData.X`/{attr}`~anndata.AnnData.layers`. {class}`~anndata.types.SupportsArrayApi` is split into a DLPack-free {class}`~anndata.types.SupportsArrayApiBase` (used to gate storage) and the DLPack-requiring {class}`~anndata.types.SupportsArrayApi` (used only where DLPack is actually needed). {user}`eroell`
