(v0.12.12)=
### 0.12.12 {small}`2026-05-06`

#### Bug fixes

- Disallow {meth}`anndata.AnnData.transpose` when `X` or `layers` contains {class}`h5py.Dataset`, {class}`zarr.Array` ,{class}`anndata.abc.CSRDataset`, or {class}`anndata.abc.CSCDataset` {user}`ilan-gold`. ({pr}`2399`)
- Don't force {attr}`anndata.settings.check_uniqueness` if user loads an annotation index with {func}`anndata.experimental.read_lazy` {user}`ilan-gold`. ({pr}`2419`)
